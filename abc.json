{
  "assets": {
    "type": "command",
    "command": {
      "cmd": [
        "tnpm install --install-node=12",
        "tnpm install -g yarn",
        "yarn config set registry https://registry.npm.taobao.org/",
        "yarn",
        "yarn run build",
        "mv ./build $BUILD_DEST"
      ]
    }
  }
}
