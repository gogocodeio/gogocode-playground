{
  "assets": {
    "type": "command",
    "command": {
      "cmd": [
        "tnpm install --install-node=12",
        "yarn",
        "yarn run build",
        "mv ./build $BUILD_DEST"
      ]
    }
  }
}
